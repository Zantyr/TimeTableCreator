definicja ajcinifed <= contruktor ostatnia zmiana - tu moze byc blad
Poprawic system in/out dla sieci - w obecnym momencie w ogole nie dziala i nie oczekuje na sygnaly
Modyfikacja callowania funkcji w taki sposób, aby przyjmowała kwargs*, gdzie wkładane są modyfikatory(wartości stringowe nie liczące się do argumentów)

HTTP/xD - technologia przyjmujaca pliki *.xD i wykonujaca obliczenia, wypluwajaca wyniki w HTML
